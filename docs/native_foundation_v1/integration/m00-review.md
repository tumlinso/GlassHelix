# M00 governance integration review

Reviewed prerequisite artifact d994a095-f737-4b69-b7f9-0a3b6dafa06e from
GH-NF1-L-A at source d7b85a797f42925906ae6b12bac8a756d885239a.
The native queue entry is 613a9114-6c46-426a-8439-6c0cc030760d, position 0;
its exact aggregate diff hash is
bd2c8a52011713a6f327c55f41b8f3ca1a7b04991ed12bf7531e49328fe8f575.
Both producer and managed destination use base
e82540220117087105f64ddedbfe1f338de683b0. Native queue application and
source-bound gate evidence, rather than this prose, prove final integration.

Accept A01's execution-toolbox scope, A02's separate GlassHelix/Cellerator
identities and two local coordinator seats, A03's conservative legacy disposition,
and A04's autonomy/recovery rules. The preserved authority observation records
GH revision 30 and CE revision 7006. These historical reads do not replace fresh
producer authority for later cross-project capability imports.

The adopted scope retains unresolved science and prohibits choosing a fibroblast
experiment or final learner. Cellerator remains numerical execution owner;
GlassHelix remains scientific description/inference owner. Legacy material is
preserved and excluded from architectural assumptions. Native claims, local lanes,
managed workspaces, scope transactions, source-linked tests and ordinary safe Git
integration apply throughout. Both programs share one GPU evidence lock plus
verified native leases; this governance milestone acquires no GPU resource.

A records include hashes of their reviewed artifacts. Validate all four records
against the integrated files. No developmental numerical interface is published
at M00: interface version/hashes are not applicable until owner publication.
No real linked numerical path exists in these governance prerequisites. M00
therefore establishes governance readiness only and does not claim a native
calculation, numerical correctness, performance, or real-consumer acceptance.
Later runtime milestones must rebuild and qualify the real linked path with
actual nonempty executable test inventory and CUDA where required.

## Early host-test registration

The controller additionally authorized a scoped root CMake entry for T01's
independent FP64 executable. Explicit NF1_HOST_VALIDATION=ON selects only the
independent test owner before legacy compiler/OpenMP/CUDA setup, requires testing
and a real gh_nf1_t01 registration, and fails if that owner is absent. Default
selection is unchanged; the eventual demo must require real CUDA. CMake minimum
is 3.28, matching installed 3.28.3; the reviewed existing configuration uses no
3.29-only feature. No biological or accelerator source is introduced here.

The missing-owner negative control was executed with CMake 3.28.3 and GNU13.3:
configuration failed at the explicit missing GH-NF1-T01 diagnostic before CUDA
setup. Positive execution awaits the real T01 fragment; no empty CTest pass or
synthetic fixture is claimed. After T01 integration, build this option and require
its real executable test inventory before dependent acceptance.
