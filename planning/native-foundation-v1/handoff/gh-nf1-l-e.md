# GH-NF1-L-E

Role: **implementer**. Project authority: `glasshelix` / `b2c4b239-f18d-4252-b388-fb9889e75efd`. Run: `GH-NF1-RUN-V1`. Workspace mode: `isolated_merge`.

This is an inert handoff until the user launches the paired controller. Register/claim/dispatch through current native first-class workflow; a local subprocess is not this lane. The single strategic controller is GlassHelix's coordinator. Any Cellerator coordinator delegation is project-qualified and does not create a foreign native lane parent.

## Queue

1. `GH-NF1-E01`
2. `GH-NF1-E02`
3. `GH-NF1-E03`
4. `GH-NF1-E04`
5. `GH-NF1-E05`

## Mission

Finite-candidate inferential state. 

Read the current task sheet and actual source before changing anything. Both explicit dependencies and this queue order apply. Producers must be integrated or supplied through an exact validated artifact before use. The initial paths are ownership reservations, not permission to duplicate engines: request autonomous scope transfer through the controller when a better existing owner is needed.

Use the pair's resource budget, separate owned worktree, validated capability receipts and integration owner. Report evidence level exactly. Do not weaken required tests, quietly skip the GPU, count syntax checks as linking, or make reference-only code a production fallback. Commit and push meaningful checkpoints, then provide source/evidence/interface hashes. Resume failed work from authority and actual files; never reset or clean unknown work.

For cross-authority blockers, continue unrelated ready work and notify the controller with the exact missing receipt/contract. Do not ask the user to approve routine choices already within the accepted program.
