# GlassHelix native execution and interrogation foundation

**Pair:** NF1-20260908-v1. **Delivery:** planning/example overlay only. **Native plan:** schema 3. No authority has been mutated and no worker has been started.

This package supplies 90 native Todo records (89 leaves plus an epic), 16 first-class lanes, seven task-owned milestone checkpoints/barriers and six developmental interfaces. Its peer is `planning/glasshelix-execution-foundation-v1` in Cellerator. Both packages are needed for the full program.

## Start here

Read `01_SCOPE_AND_DECISIONS.md`, `04_SINGLE_CONTROLLER_AND_PARALLELISM.md`, `05_CROSS_REPOSITORY_RECEIPTS.md` and `09_MANUAL_BOOTSTRAP.md`. The later execution launch file is `handoff/START_CONTROLLER.md` in GlassHelix. Do not launch workers merely by extracting or importing these files.

The historical JBC/CCP/semantic/relation packages were inspected through project-control. `12_PRECEDENT_RECONCILIATION.md` records what was reused and which historical assumptions were not copied. `07_SOURCE_LEDGER.md` is the exact-path source map.

## Native input versus explanatory files

`machine/glasshelix-native-foundation-v1.todo-plan.json` is the only native apply input. `machine/proposed_todos.json` is the richer pre-ledger with task mechanisms, acceptance, scopes and native projections. CSVs, lanes, workstreams, interfaces, barriers, context/rendezvous proposals, cross-project contracts, requirements and resource policies are consistent views, not a second live authority.

Every task has its own complete sheet in `proposed-todos/`. Every first-class lane has a queue and handoff in `handoff/`. `13_COMPLETE_TASK_CATALOG.md` provides a one-file reading copy. `index.html` provides an offline searchable human index.

## Qualification status

Full delivered graphs, queue ordering, cross-repository closure and scope overlaps are checked offline. A representative schema-3 shape was probed live without mutation. **The exact final plans still require fresh on-host native validation/diff before import.** That is mandatory in the supplied preview/apply wrapper. Actual source/compiler/GPU implementation is future work, not delivered here.

The future example APIs are guarded declaration sketches, not installed libraries. The independent mathematical reference runs now; it is not a native/GPU pass. `evidence/local_validation.json` records actual artifact checks, syntax compilation and reference results. `evidence/native_plan_validation.json` is explicitly pending, not a fabricated acceptance receipt.

## File integrity

`MANIFEST.sha256` covers every planning-package file except itself. `machine/delivery_files.json` binds the separate example overlay files. Verify before copying and before import. Hashes establish delivered-byte integrity, not the scientific truth of claims. Runtime evidence is written outside the repositories so it does not invalidate the reviewed source snapshot.

## Autonomy

Once the user launches the GlassHelix controller after manual bootstrap, it may complete the accepted foundation with first-class agents across both authorities, coordinate scope/interface changes, implement constructive Cellerator extensions, integrate, test and intermittently push. Routine engineering choices do not require per-task human approval. Preserve unknown work, actual numerical semantics and evidence; never replace missing capability with a mock or a skip.
